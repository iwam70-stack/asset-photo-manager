# 設計判断記録

## 1. 文書情報

- プロジェクト名：asset-photo-manager
- 文書状態：初期版
- 最終更新日：2026-09-01

## 2. この文書の目的

この文書は、プロジェクトの重要な選択について、結論、理由、影響、代替案を記録する。

要件の詳細は`docs/requirements.md`、現在の設計全体は`docs/design.md`、
作業状態は`docs/progress.md`を参照し、この文書には重複して記載しない。

## 3. 運用規則

- 判断IDは`DEC-001`からの連番とする。
- 状態は「提案中」「採用」「廃止」「却下」のいずれかとする。
- 採用済み判断を後から変更するときは、過去の記録を上書きせず、新しい判断を追加する。
- 新しい判断から、置き換える過去の判断IDを参照する。
- AIは判断案を作成できるが、人間の指示なしに新しい判断を「採用」にしない。
- 要件や設計の詳細を重複させず、関連文書や関連IDを参照する。

## 4. 判断記録

### DEC-001 Flutterによるスマートフォンアプリ開発

- 状態：採用
- 決定日：2026-08-30

#### 背景

備品の撮影、OCR、端末内保存、およびクラウド連携を行うスマートフォンアプリが必要であり、
初期開発で優先するプラットフォームを含めて開発基盤を定める必要がある。

#### 決定

FlutterおよびDartで開発し、初期開発ではAndroidを優先する。
iOS対応を永久に対象外とはしない。Androidの最低バージョンはこの判断では確定しない。

#### 理由

確定済みの対象環境およびプロジェクト制約に合わせ、開発基盤と初期開発の優先対象を明確にするため。

#### 主な影響

アプリの実装と検証はFlutterおよびDartを前提とし、初期開発ではAndroidでの動作を優先する。
Androidの最低バージョンに依存する設計や実装は、別途決定されるまで開始しない。

#### 検討した代替案

iOSを初期開発の優先対象とする案は、初期対象外とした。その他は、初期版では詳細な代替案の比較記録なし。

#### 関連文書・関連項目

- `docs/requirements.md`：NFR-001、制約、Q-002
- `docs/design.md`：確定している前提、使用技術候補、DQ-013

#### 置換関係

なし

### DEC-002 Google Driveをクラウド保存先とする

- 状態：採用
- 決定日：2026-08-30

#### 背景

撮影した写真をクラウドへ保存し、備品番号と写真の対応関係を維持する保存先が必要である。

#### 決定

撮影した写真のクラウド保存先としてGoogle Driveを使用する。
Google Drive以外のクラウドサービスは初期対象外とする。
Google認証方式、Googleアカウントの運用方法、Drive上の保存先、およびファイル名規則は
この判断では確定しない。

#### 理由

確定済みの保存先を明示し、初期開発で扱うクラウド連携の範囲を限定するため。

#### 主な影響

クラウド保存機能はGoogle Driveとの連携を前提とする。
認証やDrive上の具体的な保存方法に依存する設計と実装は、対応する未確定事項の決定を待つ。

#### 検討した代替案

Google Drive以外のクラウドサービスを使用する案は、初期対象外とした。
初期版ではサービスごとの詳細な比較記録なし。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-100、FR-110、制約、Q-004～Q-007
- `docs/design.md`：Google認証、Google Drive連携、DQ-004～DQ-006

#### 置換関係

なし

### DEC-003 撮影処理とアップロード処理を分離する

- 状態：採用
- 決定日：2026-08-30

#### 背景

通信や認証、アップロードの完了待ちによって、連続する備品の撮影作業を止めない構成が必要である。

#### 決定

撮影処理とアップロード処理を分離する。
利用者はアップロード完了を待たずに次の備品の撮影へ進めるものとし、
完成済みジョブをアップロード対象として管理する。
アップロードの開始方法、バックグラウンド送信の要否、自動再送と手動再送の分担は
この判断では確定しない。

#### 理由

通信状況やアップロード処理に撮影作業を直接依存させず、オフラインを含む状況でも撮影を継続できるようにするため。

#### 主な影響

撮影ジョブの完成とアップロードの進行を別に管理する必要がある。
未完成ジョブはアップロード対象にせず、完成済みジョブを送信対象として扱う。

#### 検討した代替案

各ジョブのアップロード完了後に次の撮影へ進む案は、撮影継続の要件を満たさないため採用しない。
初期版では詳細な代替案の比較記録なし。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-071、FR-090～FR-092、Q-010～Q-012
- `docs/design.md`：責務分離、アップロードキュー、DQ-008、DQ-009

#### 置換関係

なし

### DEC-004 アップロード前に撮影データを端末内へ永続保存する

- 状態：採用
- 決定日：2026-08-30

#### 背景

オフライン、アプリ終了、または端末再起動が発生しても、完成済みの未送信ジョブを失わず復旧する必要がある。

#### 決定

撮影データはGoogle Driveへ送信する前に端末内へ永続保存する。
完成済みの未送信ジョブは、アプリ終了および端末再起動後も復旧できるようにする。
ローカル永続化方式、保存期間、暗号化要否、および未完成ジョブの復旧範囲は
この判断では確定しない。

#### 理由

通信やアプリの実行状態にかかわらず、完成済みの撮影データを保全し、再送可能な状態を維持するため。

#### 主な影響

端末内への保存が完了していない撮影ジョブは完成済みまたはアップロード対象として扱わない。
完成済みの未送信ジョブを再起動後に復旧できる設計が必要になる。

#### 検討した代替案

端末内へ永続保存せず直接アップロードする案は、オフライン時の保持および再起動後の復旧要件を満たさないため採用しない。
初期版では詳細な代替案の比較記録なし。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-001、FR-080、FR-091、NFR-030、Q-013、Q-015、Q-016、Q-018、Q-026
- `docs/design.md`：ローカル保存、中断と復旧、DQ-002、DQ-010、DQ-012

#### 置換関係

なし

### DEC-005 サービスアカウント秘密鍵をアプリへ埋め込まない

- 状態：採用
- 決定日：2026-08-30

#### 背景

配布されるスマートフォンアプリから秘密情報が漏えいすることを防ぎ、
認証情報や実データをリポジトリへ含めない方針と整合させる必要がある。

#### 決定

スマートフォンアプリまたは配布物へサービスアカウント秘密鍵を埋め込まない。
認証情報および実データをGitへ含めない。
具体的なGoogle認証方式はこの判断では確定しない。

#### 理由

配布物から秘密鍵を安全に秘匿できないため。また、プロジェクトの認証情報と実データの保護要件に従うため。

#### 主な影響

サービスアカウント秘密鍵のアプリへの組み込みを前提とする認証構成は採用できない。
Google認証方式は、別途の判断と公式仕様の確認を経て決定する必要がある。

#### 検討した代替案

サービスアカウント秘密鍵をアプリまたは配布物へ埋め込む案は、秘密情報の保護方針に反するため採用しない。
初期版では詳細な代替案の比較記録なし。

#### 関連文書・関連項目

- `AGENTS.md`：セキュリティと実データ
- `docs/requirements.md`：NFR-040、NFR-041、制約、Q-005
- `docs/design.md`：Google認証、セキュリティ、DQ-004

#### 置換関係

なし

### DEC-006 備品番号を固定形式の一意な識別子として扱う

- 状態：採用
- 決定日：2026-08-31

#### 背景

OCR結果からの候補抽出、利用者が確定する値の入力検証、および撮影データの対応関係を定めるため、
備品番号の形式と運用上の一意性を明確にする必要がある。
対象は実物の備品シールの「物品番号」欄に記載された値であり、本アプリではこれを「備品番号」と呼ぶ。

#### 決定

備品番号は`NN-NNNNNN`形式とし、数字2桁、ASCIIハイフン、数字6桁で構成する。
正規表現は`^[0-9]{2}-[0-9]{6}$`とする。各数字列の先頭ゼロは有効であり、文字列として保持する。
備品番号は運用上、一つの備品を一意に識別し、異なる備品が同一番号を持つケースは考慮しない。

同じ備品を複数回撮影した場合の処理は別問題としてQ-009で管理する。
OCR結果の正規化、自動補完、および候補の優先順位はQ-028およびDQ-014で管理し、
アプリが重複を検出した場合の動作とともに、この判断では確定しない。

#### 理由

OCR候補抽出と入力検証の共通前提を定め、備品番号と一つの備品との対応を明確にするため。
また、異なる備品への番号割り当てと、同じ備品の複数回撮影に関する処理を分けて管理するため。

#### 主な影響

OCR候補抽出、入力検証、およびデータ保持は固定形式と先頭ゼロの保持を前提とする。
異なる備品に同じ番号が割り当てられる場合の処理は設計対象としない。
同じ備品の複数回撮影や重複検出時の具体的動作は、Q-009が決定するまで実装へ固定しない。

#### 検討した代替案

初期版では詳細な代替案の比較記録なし。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-030、FR-050、FR-070、Q-001、Q-008、Q-009、Q-028
- `docs/design.md`：備品番号候補抽出、撮影ジョブ、DQ-001、DQ-007、DQ-014
- `docs/test-plan.md`：備品番号候補の抽出、備品番号の確定、テストデータ

#### 置換関係

なし

### DEC-007 再撮影時は新しい完成済みデータで上書きする

- 状態：採用
- 決定日：2026-08-31

#### 背景

同じ備品を複数回撮影した場合に、既存データと新しいデータのどちらを有効とするか、および未送信・送信済みデータをどう扱うかを定める必要がある。

#### 決定

備品番号ごとに有効な撮影ジョブは1件とし、新しい撮影ジョブが完成した時点で既存データを上書きする。完成前、中断時、または保存失敗時は既存の完成済みデータを維持する。
警告や確認は行わず、過去の写真やジョブの履歴、別名保存、世代管理、および同じ備品番号の有効な別ジョブは保持しない。
未送信または送信失敗の古い送信対象は新しい完成済みジョブで置き換える。既存データが送信済みの場合は、後続アップロードによってDrive上の写真も新しい写真で置き換える。
古い送信処理と新しい撮影が重なっても、最終的に新しい完成済みジョブを有効なデータとして残す。

Drive上の具体的な置換方法、送信中ジョブのキャンセル、排他制御、送信順序、および一部送信成功時の処理は、この判断では確定しない。

#### 理由

利用者の操作とデータ管理を単純にし、備品番号ごとに有効な撮影結果を明確にするため。

#### 主な影響

新しいジョブの完成を置き換え成立の境界とし、それ以前の失敗から既存データを保護する必要がある。アップロード対象とDrive上の写真も、最終的に新しい完成済みジョブと整合させる必要がある。

#### 検討した代替案

警告して処理を選択させる案、別ジョブとして保持する案、および履歴を保持する案は、操作とデータ管理を単純にするため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-072、FR-090、FR-110、Q-009、Q-022
- `docs/design.md`：撮影ジョブ、アップロードキュー、Google Drive連携、DQ-007
- `docs/test-plan.md`：再撮影時の上書き、異常系・中断・復旧テスト

#### 置換関係

なし

### DEC-008 OCR結果を正規化し8桁数字から候補を生成する

- 状態：廃止
- 決定日：2026-08-31

#### 背景

OCRでは全角数字、ハイフン類似文字、ハイフン周辺の空白、またはハイフンを読み落とした8桁数字が得られる可能性がある。一方、シール内には備品番号以外の数字列も存在するため、形式だけで確定すると誤認するおそれがある。

#### 決定

全角数字を半角数字へ、一般的なハイフン類似文字をASCIIハイフンへ正規化し、ハイフン前後の空白を除去する。正規化後は`^[0-9]{2}-[0-9]{6}$`で検証し、先頭ゼロを保持する文字列として扱う。
OCR結果が数字8桁の場合は先頭2桁と後続6桁の間へASCIIハイフンを補い、候補を生成する。

候補は自動確定しない。利用者は前半2桁と後半6桁の数字を分かれた入力欄で確認・訂正し、ハイフンは固定表示とする。両方の桁数がそろった場合にアプリがASCIIハイフンで連結し、利用者の確認操作を経て確定する。

複数候補の優先順位、ラベルとの近さ、バーコード下の数字列の扱い、具体的な抽出アルゴリズム、類似ハイフンの厳密な一覧、画面全体、Flutterウィジェット、およびOCRライブラリの具体的な実装方法は、この判断では確定しない。

#### 理由

OCR表記の揺れやハイフンの読み落としを候補生成で吸収しつつ、備品番号以外の数字列を誤って確定しないよう、人間による確認を必須とするため。

#### 主な影響

OCR候補抽出、入力確認、および確定処理は、正規化・8桁補完・2桁と6桁の分割入力を前提とする。候補の表示だけで撮影ジョブの備品番号を確定してはならない。

#### 検討した代替案

形式に一致した候補を自動確定する案は、シール内の別の数字列を誤認する可能性があるため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-030、FR-040、FR-050、Q-028
- `docs/design.md`：OCR、DQ-014
- `docs/test-plan.md`：備品番号候補の抽出、OCR結果の確認と手修正、備品番号の確定

#### 置換関係

DEC-009により置換

### DEC-009 ハイフン付き形式だけをOCR候補とし認識できなければ手入力を求める

- 状態：採用
- 決定日：2026-08-31

#### 背景

実物の備品シールでは目的の備品番号だけがハイフン付きで印字され、通常は明瞭である。
ハイフンを認識できなかった場合に、ほかの8桁数字を加工して候補を作ると誤認のおそれがあるため、DEC-008の方針を再検討した。

#### 決定

OCR結果は、全角数字、一般的なハイフン類似文字、およびハイフン前後の空白を正規化し、`^[0-9]{2}-[0-9]{6}$`に一致したハイフン付きの値だけを候補とする。
ハイフンなしの8桁数字を補完せず、候補の順位付け、ラベルとの距離による推測、バーコード下などの数字列からの推測、および候補一覧の表示を行わない。

同一の正規化済み文字列は重複を除いて1件として扱い、有効候補がちょうど1件の場合だけ2桁欄と6桁欄へ表示する。0件または複数件の場合は、どれかを推測または選択せず、読み取れなかった旨を表示して両入力欄を空欄にし、利用者へ数字入力を求める。
ハイフンは固定表示し、両方の桁数がそろった場合にアプリがASCIIハイフンで連結する。先頭ゼロを保持し、OCR候補を自動確定しない。

正規化対象とする類似ハイフンの厳密な一覧は、この判断では確定しない。OCRライブラリ選定後に実際の出力を確認して決定する。

#### 理由

不確かな8桁数字から候補を生成するより、認識できなかったことを明示して利用者へ入力を求める方が単純で安全である。
また、小規模な個人開発で不要な候補選択処理や候補一覧UIを追加しないため。

#### 主な影響

OCR候補抽出はハイフン付き形式の正規化、検証、および重複除去を行う。
有効候補が1件の場合だけ表示し、0件または複数件の場合の処理は、候補選択ではなく空欄からの利用者入力となる。

#### 検討した代替案

ハイフンなしの8桁数字へハイフンを補って候補を生成する案は、不確かな候補を生成するため採用しない。
複数候補を順位付けして一覧表示する案は、実物シールの前提に対して不要な処理とUIを増やすため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-030、FR-040、FR-050、Q-021、Q-028、Q-029
- `docs/design.md`：OCR、DQ-014、DQ-015
- `docs/test-plan.md`：備品番号候補の抽出、OCR結果の確認と手修正、備品番号の確定

#### 置換関係

DEC-008を置換

### DEC-010 Android 7.0（API 24）を最低対応環境とする

- 状態：採用
- 決定日：2026-08-31

#### 背景

Flutterプロジェクト作成前に、Androidの最低対応バージョンと`minSdk`を定める必要がある。
現在使用を想定するFlutterの公式サポート下限と、OCRに必要なAndroid環境を満たしつつ、対応端末を不必要に狭めないことが求められる。

#### 決定

Androidの最低対応バージョンをAndroid 7.0（API 24）とし、将来作成するFlutterプロジェクトのAndroid `minSdk`を24とする。
API 23以下は対応対象に含めない。

`targetSdk`、`compileSdk`、Android SDK、Flutter SDK、Dart SDK、Gradle、Android Gradle Pluginなどの具体的なバージョンは、この判断では確定しない。

#### 理由

現在使用を想定するFlutterの公式サポート下限がAPI 24であり、API 23以上を要件とするGoogle ML KitのAndroid向け文字認識もAPI 24で利用できるため。
また、現時点では最低バージョンをさらに引き上げる必要のある機能要件や運用条件がなく、公式サポート範囲内で広い互換性を確保するため。

#### 主な影響

AndroidアプリはAPI 24以上を対象とし、API 24をインストールおよび基本動作確認の最低環境として扱う。
将来、利用端末が新しいAndroidだけに限定される場合は、最低バージョンの引き上げを再検討できる。

#### 検討した代替案

最低対応をAPI 26またはAPI 29まで引き上げる案は、現時点で引き上げを必要とする要件がなく、対応端末を不必要に狭めるため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：NFR-001、制約、Q-002
- `docs/design.md`：確定している前提、Flutterプロジェクト構成、DQ-016
- `docs/test-plan.md`：Androidエミュレーター、最低対応環境

#### 置換関係

なし

### DEC-011 初期運用を利用者1人・私物スマートフォン1台とする

- 状態：採用
- 決定日：2026-08-31

#### 背景

初期版で必要な利用者と端末の範囲を明確にし、小規模な初期運用に不要な複数利用者・複数端末向け機能を追加しないようにする必要がある。

#### 決定

初期利用者は備品管理担当者1人、初期利用端末は本人の私物スマートフォン1台とする。BYOD端末では業務用Google Workspaceアカウントを使用する。

初期版では複数利用者・複数端末による同時利用、利用者管理、役割・権限区分、同時編集の排他制御、利用者別履歴、および備品配置先職員向け機能を対象としない。

初期運用で問題がないことを確認した後、同じGoogle Workspaceドメインの職員へ展開する可能性はあるが、具体的な利用人数、端末数、権限、同時利用、および運用手順はその時点で再検討する。Google認証方式、アカウント選択、ドメイン検証、Workspace管理者側の設定、および個人用Googleアカウントの許可は、この判断では確定しない。

#### 理由

初期利用者と端末が各1つであるため、将来の可能性だけを理由に複数利用者向けの仕組みを先行実装せず、初期版の範囲を実際の運用に合わせるため。

#### 主な影響

初期設計とテストは利用者1人・端末1台を前提とする。将来の複数利用者対応を不必要に妨げないようにするが、拡張用の機能は先行実装しない。

#### 検討した代替案

初期版から複数利用者・複数端末の同時利用に対応する案は、初期運用に不要な利用者管理、権限制御、競合制御、およびテスト範囲を増やすため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：想定利用者、対象外、Q-003～Q-005
- `docs/design.md`：確定している前提、システム全体構成、Google認証、DQ-017
- `docs/test-plan.md`：対象範囲、初期運用条件、未確定事項

#### 置換関係

なし

### DEC-012 ローカル写真をアプリ専用領域で送信完了まで管理する

- 状態：採用
- 決定日：2026-09-01

#### 背景

初期運用では私物スマートフォンで業務写真を扱うため、写真が一般的なギャラリーや個人用写真バックアップへ公開されないようにしつつ、オフラインや送信失敗時のデータを保全する必要がある。

#### 決定

シール写真と全体写真は、キャッシュではないアプリ専用内部ストレージへ永続保存し、ギャラリーや共有メディア領域へ公開しない。未送信写真は固定期限を設けず、撮影ジョブ全体の正常な送信完了まで保持する。必要な写真すべてのGoogle Driveへの保存を確認した後、対応するローカル写真を即時削除する。送信開始、送信途中、送信失敗、または一部送信の状態では削除しない。

写真、ローカルジョブ情報、備品番号、送信状態、およびメタデータには、Android OS、端末、ファイルシステム、アプリ専用内部ストレージ、およびアプリ専用データ領域が提供する標準的な保護を利用し、アプリ独自の鍵や暗号化処理による追加暗号化は行わない。アプリ独自の遠隔削除、担当変更時の未送信データ移行、および端末故障・紛失時の未送信写真復元は実装しない。端末紛失または担当変更だけを理由に未送信写真を自動削除しない。

具体的な保存API、パッケージ、ディレクトリ、ファイルパス、ローカル永続化方式、送信後に残す写真以外の情報、および一部送信時の復旧・再送方法は、この判断では確定しない。OAuthトークンなどの認証情報の保存・保護方法も決定対象に含めず、未確定とする。サービスアカウント秘密鍵をアプリへ埋め込まない既存方針は維持する。

この判断でQ-015のうち確定するのはローカル写真の保存期間だけであり、送信後に写真以外のローカルジョブ情報、送信履歴、備品番号、またはメタデータを保持または削除するかは未確定とする。

#### 理由

BYOD端末の個人用写真と業務写真を分離し、一般的な写真バックアップへの意図しない公開を避けるため。Android OS、端末、ファイルシステム、およびアプリ専用領域の標準的な保護を利用することで、アプリ独自の暗号化と鍵管理の複雑さを追加せず、ローカルデータを管理できるため。

#### 主な影響

写真操作は、アプリ専用内部ストレージへの保存、未送信中の保持、ジョブ全体の送信完了確認、対応写真の削除というライフサイクルを扱う。削除後に削除済みファイルを参照しない設計とテストが必要になる。

#### 検討した代替案

ギャラリーまたは共有メディア領域へ保存する案は、BYOD端末で個人用写真や一般的な写真バックアップと混在するため採用しない。送信完了後も写真を保持する案は端末内に業務写真を残し続けるため採用しない。アプリが管理するローカルデータを独自に暗号化する案は、OS、端末、ファイルシステム、およびアプリ専用領域の標準的な保護に加えて鍵管理と復旧の複雑さを生むため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-080、FR-091、FR-131、NFR-040、Q-013、Q-015、Q-018、Q-022、Q-026
- `docs/design.md`：ローカル保存、アップロードキュー、セキュリティ、DQ-002、DQ-010
- `docs/test-plan.md`：ローカル写真の保存、保持、削除

#### 置換関係

なし

### DEC-013 各利用者本人の業務用Google Workspaceアカウントを使用する

- 状態：採用
- 決定日：2026-09-01

#### 背景

初期運用と将来の職員展開におけるGoogleアカウントの運用単位を明確にする必要がある。

#### 決定

初期運用では、備品管理担当者本人に割り当てられた通常の業務用Google Workspaceアカウントを使用する。アプリ専用または備品管理専用のWorkspaceアカウントは新設しない。

将来、同一Workspaceドメインの職員へ展開する場合も、各職員本人に割り当てられた業務用Workspaceアカウントを使用し、単一のWorkspaceアカウントを複数人で共用しない。将来の可能性だけを理由に、利用者管理や複数利用者向け機能を先行実装しない。

Google認証方式はQ-005で、Google Drive上の保存先、ファイル所有者、および共有方法はQ-006で別途決定する。認証ライブラリ、OAuthの実装方法、およびDrive APIの詳細は、この判断では確定しない。

#### 理由

現在の1人・1台運用に適合し、専用アカウントの管理負担を増やさず、アカウント共用を避けられるため。将来は各職員本人のアカウントへ自然に展開できるため。

#### 主な影響

初期運用と将来の職員展開では、利用者本人に割り当てられた業務用Workspaceアカウントを認証対象とする。認証方式とDrive上の保存・所有・共有の設計は、それぞれQ-005とQ-006の決定を待つ。

#### 検討した代替案

アプリ専用または備品管理専用のWorkspaceアカウントを新設する案と、単一のWorkspaceアカウントを複数人で共用する案は、管理負担またはアカウント共用を生むため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：想定利用者、対象外、Q-004～Q-006
- `docs/design.md`：確定している前提、Google認証、DQ-004～DQ-006
- `docs/test-plan.md`：初期運用条件、Android実機、未確定事項

#### 置換関係

なし

### DEC-014 端末上の本人認証とOAuth 2.0によるDrive認可を分離する

- 状態：採用
- 決定日：2026-09-01

#### 背景

DEC-013で確定した本人アカウント運用に対し、GoogleアカウントへのサインインとGoogle Driveへのアクセス許可の取得方法を明確にする必要がある。

#### 決定

利用者本人がスマートフォン上で本人の業務用Google Workspaceアカウントへサインインする。Googleアカウントへのサインインを認証、Google Driveへのアクセス許可の取得をOAuth 2.0による認可として分離して扱い、Driveへのアクセス許可は必要なDrive操作を行う段階で取得できる設計とする。

認証と認可はスマートフォン上で行い、独自の認証バックエンドを設けない。アプリはGoogleアカウントのパスワードを保持せず、サービスアカウントを使用しない。具体的なOAuthスコープ、Drive上の保存先、ファイル所有者、共有方法、および権限設定はQ-006で決定する。

#### 理由

DEC-013の本人アカウント運用と整合し、パスワードやサービスアカウント秘密鍵をアプリで管理せずに済むため。また、現在の小規模運用で独自バックエンドの構築・運用負担を増やさないため。

#### 主な影響

認証とDriveアクセスの認可を別の状態として扱う必要がある。認証ライブラリ、Flutterプラグイン、Androidの具体的な認証API、トークンの取得・保存・更新方法、およびGoogle Cloud側の設定詳細は、この判断では確定しない。

#### 検討した代替案

独自バックエンドでトークンを管理する方式は、小規模運用に対して構築・運用負担が増えるため採用しない。サービスアカウントを使用する方式は、DEC-013の本人アカウント運用と一致せず、秘密鍵の管理が必要になるため採用しない。

#### 関連文書・関連項目

- `docs/requirements.md`：FR-100、NFR-040、NFR-041、Q-005、Q-006
- `docs/design.md`：Google認証、DQ-004、DQ-006
- `docs/test-plan.md`：Google認証、Android実機、異常系

#### 置換関係

なし
