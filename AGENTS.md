# AGENTS.md

## 1. このファイルの目的

このファイルは、AIコーディングエージェントが本リポジトリで作業するときに
常に守るルールを定める。

アプリの要件や設計の詳細は、このファイルではなく`docs/`以下の文書で管理する。

このファイルの規則は、リポジトリ全体に適用する。

## 2. プロジェクト概要

本プロジェクトでは、備品シールと備品全体像をスマートフォンで撮影し、
OCRで備品番号を認識して、撮影データをGoogle Driveへ同期する
Flutterアプリを開発する。

初期開発ではAndroidを優先する。

Flutterプロジェクト本体は`app/`以下に配置する。

## 3. 現在の開発段階

現在は実装前の設計段階である。

明示的な指示があるまで、次の作業を行わないこと。

- Flutterプロジェクトの初期化
- Flutterコードの実装
- パッケージの追加
- Android設定の変更
- Google認証設定の作成
- Google Drive API設定の作成
- CI/CDの導入

現段階では、指定された設計文書の作成と確認を優先する。

## 4. 文書の役割

### 4.1 現在の文書

- `AGENTS.md`
  - AIコーディングエージェントが守る作業ルール
- `docs/design.md`
  - 現在採用しているシステム設計と未解決の設計課題

### 4.2 今後作成する文書

次の文書は、現時点では存在しない場合がある。

- `README.md`
  - プロジェクト概要と文書への案内
- `docs/requirements.md`
  - 機能要件、非機能要件、制約、未確定仕様
- `docs/decisions.md`
  - 重要な技術選択や設計変更の結論と理由
- `docs/progress.md`
  - 実装状況、現在の問題、次の作業
- `docs/test-plan.md`
  - テスト方針、テスト対象、実機確認項目

存在しない文書を、明示的な指示なしに作成してはならない。

文書が作成された後は、作業内容に関係する文書を確認すること。

## 5. 情報の優先順位

実装または文書変更の判断では、原則として次の順番で情報を確認する。

1. ユーザーの現在の明示的な指示
2. `docs/requirements.md`に記載された確定要件
3. `docs/design.md`に記載された現行設計
4. `docs/decisions.md`に記載された採用済み判断
5. 既存のコードとテスト
6. `docs/progress.md`に記載された現在状態

該当する文書がまだ存在しない場合は、存在する情報だけを使用する。

文書間で内容が矛盾する場合や、文書と実装が一致しない場合は、
独断でどちらかに合わせず、矛盾の内容を報告すること。

未確定事項は、既存コードや一般的な慣例だけを根拠に確定してはならない。

## 6. 作業範囲

ユーザーから指定された作業範囲だけを変更すること。

次の作業を、明示的な指示なしに追加しないこと。

- 指示されていないファイルの作成
- 指示されていない機能の実装
- 大規模なリファクタリング
- 使用ライブラリの決定または変更
- 状態管理方式の決定または変更
- ローカル永続化方式の決定または変更
- 対応プラットフォームの追加
- バックエンドサービスの導入
- CI/CDの導入
- リポジトリ構成の変更
- 無関係なファイルの整形や修正

作業中に別の問題を発見した場合は、今回の変更へ混在させず報告すること。

発見した問題が作業の安全性や正しさに直接影響する場合は、
変更を続けず、ユーザーへ確認すること。

## 7. 未確定仕様の扱い

`docs/design.md`または、今後作成する`docs/requirements.md`で
未確定とされている事項を推測で決定または実装しないこと。

特に、次の事項は明示的に確定されるまで独断で決めないこと。

- 備品番号の形式
- 対応するAndroidの最低バージョン
- 画面構成
- Google Driveの認証方式
- Googleアカウントの運用方法
- Drive上の保存先
- Drive上のファイル名規則
- 重複する備品番号の処理
- 再撮影時の処理
- 上書きまたは別名保存の選択
- アップロード開始方法
- バックグラウンド送信の要否
- 再送方式
- アップロード後のローカル画像の扱い
- 写真の解像度、圧縮率、保存期間
- ローカル永続データの管理方式
- 状態管理方式
- `app/lib/`以下のディレクトリ構成

作業に未確定事項の判断が必要になった場合は、次を報告して停止すること。

1. 判断が必要な事項
2. 選択肢
3. 各選択肢の影響
4. 推奨案とその理由

推奨案を提示することと、推奨案を採用済みとして実装することを混同しないこと。

## 8. 変更前の確認

ファイルを変更する前に、少なくとも次を行うこと。

1. `git status`で作業ツリーを確認する。
2. 今回の作業に関係する既存文書を読む。
3. 変更対象を確認する。
4. 変更してはいけない範囲を確認する。
5. 未コミット変更が作業対象と重ならないか確認する。
6. 完了条件と必要な確認方法を確認する。

既存の未コミット変更は、ユーザーが作成した変更として扱うこと。

既存変更を勝手に破棄、上書き、ステージング、コミットしてはならない。

作業対象と既存変更が重なる場合は、変更を始める前に報告すること。

Gitリポジトリがまだ初期化されていない場合は、
明示的な指示なしに`git init`を実行しないこと。

## 9. 実装方針

実装段階へ進んだ後は、次の方針を守ること。

- FlutterとDartの標準的な構成に従う。
- 初期開発ではAndroidでの動作を優先する。
- 必要以上に抽象化しない。
- 将来の可能性だけを理由に仕組みを追加しない。
- 既存の実装パターンがある場合は、それに合わせる。
- 複数の責務を一つのクラスや関数に集中させない。
- 撮影処理とアップロード処理を分離する。
- 撮影データは、アップロード前に端末内へ永続化する。
- アプリ終了や端末再起動後も、未送信ジョブを復旧できる設計を維持する。
- ファイル名だけを撮影ジョブの管理手段にしない。
- エラーを無視して処理を成功扱いにしない。

パッケージやAPIの仕様が現在も有効か不明な場合は、
公式ドキュメントを確認してから提案または実装すること。

候補として記載されているパッケージを、採用済みとして扱わないこと。

## 10. セキュリティと実データ

次の情報をソースコード、テストコード、文書、Git履歴へ含めないこと。

- APIキー
- OAuthトークン
- サービスアカウント秘密鍵
- その他の認証情報
- 実在する個人情報
- 実際の備品写真
- 実在する備品番号を含む検証データ

スマートフォンアプリ内へサービスアカウントの秘密鍵を埋め込んではならない。

テストでは、人工的に作成した備品番号とサンプル画像を使用すること。

秘密情報らしきものを発見した場合は、内容を回答へ転載せず、
対象ファイルと問題の種類だけを報告すること。

## 11. テストと検証

実装段階へ進んだ後は、変更内容に応じて必要なテストを実行すること。

Flutterコードを変更した場合は、原則として次を確認する。

- Dartコードのフォーマット
- `flutter analyze`
- 関係する単体テストまたはWidgetテスト
- 必要に応じて`flutter test`

実際に使用するコマンドは、Flutterプロジェクト初期化後の構成に合わせること。

統合テストや実機確認が必要だが実行できない場合は、
実行したと偽らず、未実施の理由と人間が確認すべき内容を報告すること。

テスト失敗を、今回の変更と無関係だと根拠なく判断しないこと。

テストを通すためだけに、要件を弱めたりテストを削除したりしてはならない。

文書だけを変更した場合は、文書内のリンク、用語、見出し、
相互参照およびMarkdown構造を確認すること。

## 12. 文書更新

実装または設計を変更した場合は、関連文書の更新要否を確認すること。

各文書が作成された後は、次の役割分担を守る。

- 要件の変更：`docs/requirements.md`
- 現行設計の変更：`docs/design.md`
- 重要な選択や変更理由：`docs/decisions.md`
- 実装状況や次の作業：`docs/progress.md`
- テスト方針の変更：`docs/test-plan.md`

同じ詳細説明を複数文書へ重複して記載しないこと。

`docs/progress.md`を恒久的な仕様書として使用してはならない。

重要な設計変更は、実装変更と可能な限り別の作業、別のコミットにすること。

存在しない文書を、関連文書という理由だけで自動的に作成してはならない。

## 13. Git運用

作業開始前と作業終了後に`git status`を確認すること。

Gitリポジトリが存在する場合は、必要に応じて`git diff`を使用して、
今回の変更内容を確認すること。

新規の未追跡ファイルは通常の`git diff`へ表示されないため、
未追跡ファイルの存在と内容を別途確認すること。

作業終了後は、少なくとも次を報告すること。

- 変更または作成したファイル
- 変更内容の要約
- 実行した確認またはテスト
- 確認またはテストの結果
- 未実施の確認事項
- 残っている問題
- `git status`の概要

次の操作は、ユーザーから明示的な指示がある場合だけ実行すること。

- `git init`
- `git add`
- `git commit`
- `git push`
- ブランチの作成または切り替え
- タグの作成

次の操作は原則として実行しないこと。

- `git reset --hard`
- `git clean -fd`
- 強制push
- コミット履歴の書き換え
- 既存変更の破棄

復旧操作が必要な場合は、現在の状態と復旧方法の候補を提示し、
ユーザーの確認を得ること。

## 14. 作業完了の条件

次の条件を満たした場合だけ、作業完了として報告すること。

- 指示された変更が実施されている。
- 指示されていない範囲を変更していない。
- 必要なテストまたは確認を実施している。
- 実装と既存文書に既知の矛盾がない。
- 未実施項目と既知の問題を明示している。
- コミットしていない変更の状態を報告している。

テスト未実施、実機未確認、仕様未確定などが残る場合は、
「すべて完了」と断定せず、その範囲を明示すること。
